# Graphs for a Quantitative Variable

To display data from one quantitative variable graphically, we can use either the {term}`histogram` or the {term}`stemplot`. (Another graph, the {term}`boxplot`, will be mentioned later in this module).
